import {
  Tabs,
  TabsContent,
  TabsList,
  TabsTrigger,
} from "@components/Tabs/Tabs";

import {
  ComponentDemo,
  ComponentSourceTabs,
  ComponentExamples, DocPre
} from "src/docs/components/MdxComponents";

# {props.title}

 {props.description}

<ComponentDemo type={props.slug} />

## Installation

If you haven't initialized MailingUI in your project, please refer to [Installation](/docs/installation) docs first.

Choose an a) automatic install by running our `@mailingui/cli` following the commands/options provided or b) install manually by copying and pasting directly into your `src/mailingui/components` directory.

  <Tabs defaultValue="automatic">
    <div className="flex items-center justify-start w-full mb-8">
      <TabsList className="mt-8 rounded-2xl">
        <TabsTrigger value="automatic" className="px-7 py-2 rounded-[10px]">Automatic Install</TabsTrigger>
        <TabsTrigger value="manual" className="px-7 py-2 rounded-[10px]">Manual Install</TabsTrigger>
      </TabsList>
    </div>

    <TabsContent value="automatic">
      <pre>
        <code>
<DocPre children={`npx @mailingui/cli add [options] ${props.installName}

# Options
-o, --overwrite  automatically overwrite existing components (default: false)
-a, --all        download all available components (default: false)
-h, --help       display help for command`} />
        </code>
      </pre>
    </TabsContent>
    <TabsContent value="manual">
    <ComponentSourceTabs type={props.slug} />
    </TabsContent>

  </Tabs>

## Examples

<ComponentExamples type={props.slug} />
