import { getTemplateProps } from "@utils/templates";
import { Header } from "@components/templates/Header.tsx";
import { Examples } from "@components/templates/examples/Examples.tsx";

export const getStaticProps = async () => {
  const { type, html, demoCode, sourceCode, examples } = await getTemplateProps(
    {
      template: "minimal",
      category: "events"
    }
  );

  return {
    props: {
      ssg: {
        examples,
      },
    },
  };
};

<div className="pt-2">
  <Header
    tag="Minimal Templates"
    github="MailingUI"
    title="Events"
    description="Events are a great way to get people together. Can be conference, a meetup, or a party."
    downloadHref="https://minhaskamal.github.io/DownGit/#/home?url=https://github.com/webscopeio/mailingui/tree/main/src/emails/minimal/events"
  />
</div>

<Examples />
