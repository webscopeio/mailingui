---
title: Badges - Components
description: Build eye-catching emails with pre-made components
---

import { getComponentProps } from "@utils/components";

import { Installation } from "@components/docs/Installation";
import { Example } from "@components/docs/Example";

export const getStaticProps = async () => {
  const { sourceCode, examples } = await getComponentProps("badge", "Badge.tsx");

  return {
    props: {
      ssg: {
        sourceCode,
        examples,
      },
    },
  };
};

# Badges

Highlight key information with attention-grabbing badges

## Basic Usage

<Example name="Demo" height={120} />

## Installation

<Installation>
  ```bash copy
  npx @mailingui/cli add [options] badge
  ```
</Installation>

## Examples

### Demo

<Example name="Demo" height={120} />
