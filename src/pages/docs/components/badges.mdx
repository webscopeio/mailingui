---
title: Badges - Components
description: Build eye-catching emails with pre-made components
---

import { getComponentProps } from "@utils/components";
import { BasicUsage } from "@components/docs/BasicUsage";
import Installation from "@components/docs/installation.mdx";
import { Examples } from "@components/docs/Examples";

export const getStaticProps = async () => {
  const { type, html, demoCode, sourceCode, examples } =
    await getComponentProps({
      type: "badge",
    });

  return {
    props: {
      ssg: {
        type,
        html,
        demoCode,
        sourceCode,
        examples,
      },
    },
  };
};

# Badges

Highlight key information with attention-grabbing badges

## Basic Usage

<BasicUsage />

## Installation

<Installation cli="badge" />

## Examples

<Examples />
