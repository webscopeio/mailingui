---
title: Emojis - Components
description: Build eye-catching emails with pre-made components
---

import { getComponentProps } from "@utils/components";
import { BasicUsage } from "@components/docs/BasicUsage";
import Installation from "@components/docs/installation.mdx";
import { Examples } from "@components/docs/Examples";

export const getStaticProps = async () => {
  const { type, html, demoCode, sourceCode, examples } = await getComponentProps({
    type: "emoji"
  }) 

  return {
    props: {
      ssg: {
        type,
        html,
        demoCode,
        sourceCode,
        examples
      },
    },
  };
};

# Emojis

Add personality and emotion to your messages

## Basic Usage

<BasicUsage />

## Installation

<Installation cli="emoji" />

## Examples

<Examples />
