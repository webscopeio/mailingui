---
title: Markdown - Components
description: Use the power of MDX to author your emails
---

import { getComponentProps } from "@utils/components";

import { Installation } from "@components/docs/Installation";
import { Example } from "@components/docs/Example";

export const getStaticProps = async () => {
  const { sourceCode, examples } = await getComponentProps("markdown", "Markdown.tsx");

  return {
    props: {
      ssg: {
        sourceCode,
        examples,
      },
    },
  };
};

# Markdown

Use the power of MDX to author your emails

## Basic Usage

<Example name="Demo" hasMDX />

## Installation

<Installation>
  ```bash copy
  npx @mailingui/cli add [options] markdown
  ```
</Installation>

## Examples

### Custom Styles

<Example name="CustomStyles" height={600} hasMDX />

### Newsletter

<Example name="Newsletter" height={600} hasMDX />
